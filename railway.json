{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo ready"
  },
  "deploy": {
    "startCommand": "cd server && python -u app.py",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
